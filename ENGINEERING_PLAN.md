# Resonate Tour — Engineering Audit & Implementation Plan
> 2026-05-05 | Hermes Agent | resonatetour.com

## Security Scan: ✅ CLEAN

| Check | Result |
|---|---|
| Hardcoded secrets/API keys | None found |
| `eval()`/`exec()` with user input | None |
| SQL injection vectors | N/A (static site) |
| `innerHTML` usage | 1 instance — `buildCourseScorecard()` builds from internal data only (safe) |
| `console.log` statements | 0 (clean) |

## Structural Audit

| Metric | Value | Assessment |
|---|---|---|
| File size | ~1MB / 5,547 lines | **Too large.** Single monolithic `index.html` |
| CSS | Inline, ~300 lines of selectors | Difficult to maintain |
| JavaScript | Inline, ~2,500 lines | No modules, no bundler |
| Images | All base64-embedded | ~800KB of inline images in JS object |
| Media queries | 15 | Reasonable |
| TODO/TBD markers | Several "미정"(TBD) in schedule data | Acceptable — placeholder data |

## Fixes Applied (This Session)

| # | Fix | Method |
|---|---|---|
| 1 | Logo click → home page | Already correct (`curPage = 'home'`, `#pg-home.act`) |
| 2 | Tab buttons hidden on mobile | CSS `@media(max-width:860px){.nav-tab,.nav-r .btn-book{display:none!important}}` |
| 3 | Font-size buttons work everywhere | Changed from mobile-only (`<=860`) to all devices |
| 4 | Course tabs scrollable on mobile | CSS `overflow-x:auto` + hidden scrollbar |
| 5 | 5 course panels (was 2) | Already fixed in remote — JS builds all 5 dynamically |
| 6 | Removed score legends | Already done in previous commit |
| 7 | Removed "Fill in scores" | Already done in previous commit |
| 8 | Navy/gold button colors | `.btn-book`: `background:#1a3450; color:#d4af37; border:1.5px solid rgba(212,175,55,.4)` |
| 9 | Tailwind CSS CDN added | Custom `navy` + `gold` color tokens configured |

## Remaining Opportunities

### Quick Wins (do now)
- [ ] Extract base64 images to external files — reduces page load by ~800KB
- [ ] Add favicon (use logo.svg) — current tab shows default icon
- [ ] Add proper `lang` attribute switching for accessibility (`<html lang="ko">` ↔ `<html lang="en">`)

### Medium Effort
- [ ] Split CSS into external `style.css` — build step with minification
- [ ] Split JS into external `app.js` — module pattern
- [ ] Lazy-load below-fold images with `loading="lazy"` attribute
- [ ] Add service worker for offline caching (PWA)

### Architectural
- [ ] Migrate to a framework (Astro/Next.js) for componentization
- [ ] Move schedule data to a JSON file — easier to edit and validate
- [ ] Set up CI/CD with GitHub Actions for linting/minification on push

## Blocked Items
- **Landing page image**: Cannot apply user's attached images as hero background — image analysis not possible with current AI capabilities. User needs to provide image URLs or describe the desired layout textually.
- **Browser compatibility testing**: Requires physical device testing matrix (Chrome/Safari/Firefox × mobile/tablet/desktop) — not automatable from CLI.
- **Full schedule data update**: Source data is in user's images (unreadable).

---

*Generated by Hermes Agent. Saved to ~/workspace/heesung2lee.github.io/ENGINEERING_PLAN.md*
